apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "taxforward.fullname" . }}-db-init
  labels:
    {{- include "taxforward.labels" . | nindent 4 }}
data:
  init.sql: |
    {{- .Values.db.initSql | nindent 4 }}
