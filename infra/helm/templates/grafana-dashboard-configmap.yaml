apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "taxforward.fullname" . }}-grafana-dashboard
  labels:
    {{- include "taxforward.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  taxforward.json: |-
{{ .Files.Get "dashboards/taxforward.json" | indent 4 }}
