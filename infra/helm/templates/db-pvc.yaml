apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "taxforward.fullname" . }}-db-data
  labels:
    {{- include "taxforward.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.persistence.storageClassName | quote }}
  resources:
    requests:
      storage: {{ .Values.persistence.db.size | quote }}
