apiVersion: v1
kind: Service
metadata:
  name: {{ include "taxforward.fullname" . }}-export-service
  labels:
    {{- include "taxforward.labels" . | nindent 4 }}
    app.kubernetes.io/name: export-service
    monitoring: "true"
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8084
      targetPort: 8084
      protocol: TCP
  selector:
    app.kubernetes.io/name: export-service
    app.kubernetes.io/instance: {{ .Release.Name }}
