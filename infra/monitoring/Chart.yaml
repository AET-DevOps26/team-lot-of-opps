apiVersion: v2
name: taxforward-monitoring
description: Monitoring stack for TaxForward (Prometheus + Grafana + AlertManager + Loki)
type: application
version: 0.1.0
dependencies:
  - name: kube-prometheus-stack
    version: ">=60.0.0"
    repository: https://prometheus-community.github.io/helm-charts
  - name: loki-stack
    version: ">=2.9.0"
    repository: https://grafana.github.io/helm-charts
