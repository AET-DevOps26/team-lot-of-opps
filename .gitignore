.vscode/
.DS_Store
**/.env
__pycache__/
devops-*firebase*.json
firebase-debug.log
values.local.yaml
stud.yaml

# Generated from api/openapi.yaml (see api/scripts/gen-all.sh).
# client/src/api/schema.ts is committed (the client imports it); a CI drift
# check keeps it in sync with the spec. Language-specific stubs are not.
api/generated/
