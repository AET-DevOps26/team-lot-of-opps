[tool.pytest.ini_options]
markers = [
    "integration: requires a running llm-chat service, Postgres, and a real LLM; not run by default (see tests/test_integration_rag.py)",
]

[tool.ruff]
line-length = 100

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
