fastapi
uvicorn[standard]
firebase-admin
# Pin away from cryptography 48.x: its bundled OpenSSL crashes (SIGILL)
# inside the Docker Desktop VM on Apple Silicon. 44.x ships a compatible wheel.
cryptography==44.0.2
# OpenTelemetry: `opentelemetry-instrument` auto-instruments FastAPI and outbound
# HTTP, pushing metrics + traces over OTLP to the collector (see Dockerfile).
opentelemetry-distro
opentelemetry-exporter-otlp
opentelemetry-instrumentation-fastapi
