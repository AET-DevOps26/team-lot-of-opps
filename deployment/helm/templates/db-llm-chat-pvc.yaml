apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "taxforward.fullname" . }}-db-llm-chat-data
  labels:
    {{- include "taxforward.labels" . | nindent 4 }}
    app.kubernetes.io/name: db-llm-chat
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.dbLlmChat.size | quote }}
