apiVersion: v1
kind: Service
metadata:
  name: {{ include "taxforward.fullname" . }}-db-llm-chat
  labels:
    {{- include "taxforward.labels" . | nindent 4 }}
    app.kubernetes.io/name: db-llm-chat
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app.kubernetes.io/name: db-llm-chat
    app.kubernetes.io/instance: {{ .Release.Name }}
